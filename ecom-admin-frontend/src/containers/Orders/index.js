import React from 'react';
import Layout from '../../components/Layout';

function Order(props) {
    return(
        <Layout sidebar>
            Orders
        </Layout>
    );
}

export default Order;